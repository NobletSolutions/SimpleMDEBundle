{% block simplemde_editor_row %}
    <div class="form-group">
        {{ form_label(form) }}
        {{ form_widget(form) }}
        {{ form_errors(form) }}
    </div>
{% endblock %}

{% block simplemde_editor_widget %}
    {{ block('textarea_widget') }}
    <script type="text/javascript">
        $('document').ready(function()
        {
            var simplemde_test = new SimpleMDE({ element: $("#{{ id }}")[0] });
        });
    </script>
{% endblock %}
